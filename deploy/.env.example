# A hostname whose DNS points at the API server, without https://.
API_DOMAIN=api.example.org
# Absolute directory on that server containing the prepared .cache contents.
DYNAWORD_DATA_DIR=/srv/dynaword/data
PAGES_ORIGIN=https://v4ldesalnikov.github.io
